package UiServices;

import android.content.Context;
import android.support.annotation.NonNull;
import android.support.v4.view.ViewPager;

public class LoginPager extends ViewPager {
    public LoginPager(@NonNull Context context) {
        super(context);
    }
}
